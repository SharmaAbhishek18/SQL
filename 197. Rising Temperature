SELECT w1.id
FROM Weather w1
INNER JOIN Weather w2
-- For each row in w1 (today), it finds a row in w2 (yesterday) such that:
WHERE DATEDIFF(w1.recordDate , w2.recordDate) = 1
AND w1.temperature > w2.temperature
