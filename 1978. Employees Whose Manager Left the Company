# Write your MySQL query statement below
Select employee_id
FROM Employee
WHERE salary < 30000 AND 
    manager_id NOT IN (
        SELECT employee_id
        FROM Employee
    )
ORDER BY employee_id
